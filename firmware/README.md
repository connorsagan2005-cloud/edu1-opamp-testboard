# firmware

Owner: TBD. Status: not started.

FPGA (Basys 3) code that drives the EDU1 5-pin serial scan chain
(`sin` / `sclk` / `srstb` / `slatch` / `ssel`) to configure blocks during testing,
plus any test-automation firmware. Expected contents: HDL sources, constraints, and a
short doc mapping scan-chain bit positions to block configuration fields.
