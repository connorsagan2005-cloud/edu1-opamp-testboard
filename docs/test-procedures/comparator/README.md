# docs/test-procedures/comparator

Owner: TBD. Status: not started.

Will hold the comparator block's finalized test-circuit spec and deviations doc.
Standards research not finalized — see [`docs/standards/INDEX.md`](../../standards/INDEX.md).

Note: the comparator shares an unclocked-comparator input stage with the op-amp; its
output is digital, so propagation-delay / timing methods apply in addition to the
analog input-referred methods.
